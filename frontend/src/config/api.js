// Centralized API base URL
// Change this one variable to switch between local and production
const API_BASE_URL = import.meta.env.VITE_API_URL || 'https://elite-engineers-9rt9.onrender.com';

export default API_BASE_URL;
